apiVersion: v1
kind: Service
metadata:
  name: learninghub-service
spec:
  type: NodePort
  selector:
    app: learninghub
  ports:
    - port: 5174        # Kubernetes service port
      targetPort: 5174  # Matches containerPort
      nodePort: 30080   # External access (http://localhost:30080)
